<!-- res/drawable/dialog_rounded_corners_with_shadow.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#E1000000" /> <!-- Shadow color with alpha -->
            <corners android:radius="16dp" /> <!-- Corner radius in dp -->
        </shape>
    </item>
    <!-- Content Background -->
    <item android:right="4dp" android:left="4dp" android:top="4dp" android:bottom="4dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" /> <!-- Background color -->
            <corners android:radius="16dp" /> <!-- Corner radius in dp -->
        </shape>
    </item>
</layer-list>
